{
  "name": "torqueon-nativo",
  "private": true,
  "version": "1.0.2",
  "description": "App nativo do TORQUE ON (Capacitor) — empacota o sistema em um app Android/iOS de verdade",
  "scripts": {
    "www": "node copia-www.js",
    "android": "npm run www && npx cap add android && npx cap sync android",
    "ios": "npm run www && npx cap add ios && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
