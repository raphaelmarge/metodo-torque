{
  "name": "agente-sessions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "App mínimo que conversa com um agente gerenciado da Anthropic (sessions + stream de eventos)",
  "scripts": {
    "start": "node --experimental-strip-types index.ts",
    "typecheck": "tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext --skipLibCheck index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.0"
  }
}
